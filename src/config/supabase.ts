// [GREY] Supabase client — server-side only (SERVICE_ROLE_KEY always)
// Re-exports from lib/supabaseServer for path consistency

export { supabaseAdmin, supabaseAdminConfigured } from '../lib/supabaseServer.js';
